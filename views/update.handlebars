<h2 class="page-header">Updated Event</h2>
<table id="updateEvents" >
    <tr>
        <th>ID</th>
        <th>Activity</th>
        <th>Date</th>
        <th>Start Time</th>
        <th>End Time</th>
    </tr>
    <tr>
        <td class="id-cell">{{data.objID}}</td>
        <td>{{data.activity}}</td>
        <td>{{data.date}}</td>
        <td>{{data.start}}</td>
        <td>{{data.end}}</td>
      
        <td class="button-cells"><form method="post" action="/search/edit">
            <button id="toBeUpdated" value="{{data.objID}}" name="toBeUpdated" type="submit" class="btn btn-default edit-del hover-blue">Edit</button>
            </form></td>
            
        <td class="button-cells"><form method="post" action="/search/delete">
        <input type="hidden", name="_method", value="delete"/>
        <a href="/search/delete?id={{data.objID}}" onclick='return confirm("You are going to delete this event. Continue?")'class="btn btn-default edit-del hover-red">Delete</a>
        </form>
        </td>
    </tr>
</table>

<h3 class="green">Your changes have been updated.</h3>